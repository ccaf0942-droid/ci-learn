name: defolt

on:
    push:
        branches: [master]

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: ls -la
            - run: python --version
            - run: python check.py




            



