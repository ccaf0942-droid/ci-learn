name: basa

on:
    push:
        branches: [master]

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: python info.py
            - run: python files.py
            
    network:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: pip install --no-cache-dir -r reqmest.txt
            - run: python ping.py



            



