name: basa

on:
    push:
        barnches: [master]

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: docker build -t pings .
            - run: docker images
            # запускаем контейнер после просмотра то что он собрался
            - run: docker run --rm pings

            



